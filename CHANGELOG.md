# Changelog

## Unreleased

- Move editable Sphinx sources to `docs_src/` and generate GitHub Pages-ready HTML directly under `docs/`.
- Document the ``class`` field on HF conversation JSONL rows (question-type / template name) in data curation and SFT guides.
- Document corrected ``server_winner`` / ``receiver_winner`` MCQ answer format (full player-name combinations for server/receiver and winner).

## 1.0.0

- Initial **Sports Intelligence Cookbooks** documentation repository based on the NVIDIA `repo_docs` template.
- Single docs project key: `sports_intelligence_stack` (build output path under `_build/docs/`); display name: Sports Intelligence Cookbooks.
- Vendored builder renamed to `doc_build_toolchain/repo/docs/`.
